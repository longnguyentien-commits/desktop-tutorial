{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start local game server",
            "type": "shell",
            "command": ".\\server.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "isBackground": true,
            "problemMatcher": []
        }
    ]
}
